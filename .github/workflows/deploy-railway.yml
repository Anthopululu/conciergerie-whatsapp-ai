name: Deploy to Railway

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy to Railway
        uses: bervProject/railway-deploy@v0.3.4
        with:
          railway_token: ${{ secrets.RAILWAY_TOKEN }}
          service: conciergerie-backend
          # Railway détectera automatiquement les services depuis le repository

